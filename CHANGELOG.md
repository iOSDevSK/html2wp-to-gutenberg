# Changelog

By commit, newest first. The subjects are the log's own; the body of each commit holds the evidence.

## Unreleased

- Documentation for people (`README.md`, `docs/`, `CONTRIBUTING.md`, this file); licence changed from MIT to GPL-2.0-or-later so what the skill carries into a theme stays compatible with WordPress; `1,303 blocks / 0 invalid` recorded where the description and verification.md still said 1,025.

## 2026-09-15

- `3737061` A fix to the writer fixes the next import only; pitfalls 1e and 5d now say what reaches the sites already written
- `5ac9e0e` The harness moves into the skill, and running it finds two more ways an import goes quietly wrong
- `acd26a4` The mapping table kept what pitfall #1c said to drop, and the gates could pass on zero files

## 2026-09-14

- `7a092b5` A form's delivery choice is three more attributes, and one of them is a relay
- `843eada` The reference conversion is the worked example again
- `1c7f39b` Forms the client can edit, and an SEO panel that is wired to something

## 2026-08-23

- `4e1825b` A navigation block with inline links is not a menu
- `f96b4b9` An importer that must finish inside one request will not

## 2026-08-20

- `0f0750d` Every generated theme must be able to take its import back off
- `6624d70` Pitfalls: stored-but-not-serialized supports, className's element, and going beyond core storage
- `ffb5a94` Pitfalls: the allow-list that drops instead of refusing, and fluid typography
- `f098989` Pitfalls: containers, deprecated saves, and reading the block registry

## 2026-08-19

- `ceb1cd2` Dress the post-content placeholder slot; :has([data-block]) guards real content
- `f30092c` core/html previews are a third document editor.css cannot reach
- `bb2b14a` Add the editor-DOM traps, and the one where the symptom lies
- `2a9f2e7` Add what the reference conversion got wrong after it was "finished"
- `408d456` html2wp → native Gutenberg conversion skill
